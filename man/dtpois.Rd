% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tpoiss.R
\name{dtpois}
\alias{dtpois}
\alias{ptpois}
\alias{qtpois}
\alias{rtpois}
\title{The Truncated Poisson Distribution}
\usage{
dtpois(x, lambda, lower, upper, log = FALSE)

ptpois(q, lambda, lower, upper, lower.tail = TRUE, log.p = FALSE)

qtpois(p, lambda, lower, upper, lower.tail = TRUE, log.p = FALSE)

rtpois(n, lambda, lower, upper)
}
\arguments{
\item{x}{vector of (non-negative integer) quantiles.}

\item{lambda}{vector of (non-negative) Poisson means.}

\item{lower}{vector of lower truncation bounds (exclusive).}

\item{upper}{vector of upper truncation bounds (inclusive).}

\item{log}{logical; if \code{TRUE}, probabilities \code{p} are given as \code{log(p)}.}

\item{q}{vector of quantiles.}

\item{lower.tail}{logical; if \code{TRUE} (default), probabilities are \eqn{P[X \le x]}{P[X <= x]},
otherwise, \eqn{P[X > x]}.}

\item{log.p}{logical; if \code{TRUE}, probabilities \code{p} are given as \code{log(p)}.}

\item{p}{vector of probabilities.}

\item{n}{number of observations. If \code{length(n) > 1}, the length is taken to be the number required.}
}
\value{
\code{dtpois} gives the density.

\code{ptpois} gives the distribution function.

\code{qtpois} gives the quantile function.

\code{rtpois} generates random deviates.
}
\description{
Density, distribution function, quantile function, and random generation
for the truncated Poisson distribution with parameter \code{lambda},
truncated to the interval \code{(lower, upper]}.
}
\details{
The truncated Poisson distribution has density
\deqn{f(x) = \frac{P(X = x)}{P(a < X \le b)}}{f(x) = P(X = x) / P(a < X <= b)}
for \eqn{x \in \{a+1, a+2, \ldots, b\}}{x in {a+1, a+2, ..., b}}, where \eqn{X \sim \text{Poisson}(\lambda)}{X ~ Poisson(lambda)},
\eqn{a} is the \code{lower} bound and \eqn{b} is the \code{upper} bound.
}
\examples{
# Density of truncated Poisson (lambda=5, truncated to (0, 10])
dtpois(1:10, lambda = 5, lower = 0, upper = 10)

# CDF of truncated Poisson
ptpois(3, lambda = 5, lower = 0, upper = 10)

# Quantile function of truncated Poisson
qtpois(0.5, lambda = 5, lower = 0, upper = 10)

# Random generation from truncated Poisson
rtpois(10, lambda = 5, lower = 0, upper = 10)

}
\seealso{
\code{\link{ptpois}}, \code{\link{qtpois}}, \code{\link{rtpois}}
}
